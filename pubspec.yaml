name: portfolio
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  url_launcher: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0




flutter:

  uses-material-design: true
  assets:
    - assets/image.jpg
    - assets/android.png
    - assets/angular-file.png
    - assets/ca.jpg
    - assets/facebook.png
    - assets/github.png
    - assets/instagram.png
    - assets/java-script.png
    - assets/java.png
    - assets/linkedin.png
    - assets/police.jpg
    - assets/python.png
    - assets/sql.png
    - assets/typescript.png
    - assets/webdev.png
    - assets/assistant.jpeg
    - assets/react.png
    - assets/azure.png
    - assets/docker.png
    - assets/ecommerce.png
    - assets/news.png
    - assets/cinemate.png

